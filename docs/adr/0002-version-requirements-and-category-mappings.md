# Version requirements and category mappings together

Each requirement baseline owns an explicit, versioned category mapping because course labels and graduation rules can vary by cohort and major. The calculator does not infer mappings from keywords: unknown categories remain unclassified. Baseline versions are preserved rather than overwritten, and switching versions recalculates progress without altering the underlying academic records.
