# Use React for the course planning interface

The planning interface will use React, TypeScript, and Vite while Flask remains a JSON API adapter and serves the production build. The weekly timetable, overlaid candidate sections, conflict explanations, priority editing, and asynchronous task progress form a stateful interaction model that would otherwise force substantial application logic into server-rendered templates; generated static assets still preserve a single local start command for users.
