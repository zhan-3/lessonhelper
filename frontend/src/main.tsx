import React,{useEffect,useState} from "react";import{createRoot}from"react-dom/client";import type{WorkbenchState}from"./api.generated";import"./style.css";
function App(){const[state,setState]=useState<WorkbenchState|null>(null);useEffect(()=>{fetch("/api/state").then(r=>r.json()).then(setState)},[]);if(!state)return <main>正在读取本地工作区…</main>;const t=state.snapshots.timetable;const s=state.snapshots.selection;return <main><header><p className="eyebrow">READ-ONLY ACADEMIC WORKBENCH</p><h1>选课规划工作台</h1><span className="session">教务会话 · {state.academic_session.state}</span></header><section className="facts"><article><small>当前画像</small><strong>{String(state.profile?.grade??"尚未配置")} 级</strong></article><article><small>已确认通知</small><strong>{String(state.confirmed_notice?.title??"尚未确认")}</strong></article><article><small>当前学期</small><strong>{t?.term||s?.term||"—"}</strong></article></section><section><h2>每周课表</h2>{t?<div className="week">{[1,2,3,4,5,6,7].map(day=><div><b>星期{day}</b>{((t.payload.entries as any[])||[]).filter(x=>x.weekday===day).map((x,i)=><p key={i}>{x.start_period}-{x.end_period} {x.course_name}</p>)}</div>)}</div>:<p className="empty">暂无完整课表快照。页面读取不会连接学校系统。</p>}</section><section><h2>待选课程班</h2><p>{s?`${(s.payload.sections as any[])?.length??0} 个课程班 · ${s.source_at}`:"暂无完整待选课程快照"}</p></section></main>}createRoot(document.getElementById("root")!).render(<App/>);
