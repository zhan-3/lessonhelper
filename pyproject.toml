[project]
name = "lab-scraper"
version = "0.1.0"
description = "HITWH 开放式实验系统抢课自动化工具 | CAS SSO 登录 + Playwright 人工辅助登录 + 会话持久化 + 自动预约 + 监控模式"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.58.0",
]
